# Never let TLS material into the capsule repo (certs/keys live in
# /etc/molly-brown, but guard against an accidental copy landing here).
*.key
*.crt
*.pem

# Editor/OS cruft
*~
.*.swp
*.kate-swp
.DS_Store
