# /run/status/starport — where statusPull.service leaves starport's vitals for
# status.cgi. Installed as /etc/tmpfiles.d/statusPull.conf by promoteSite.sh.
# The parent /run/status is created by status.conf (tmpfiles orders by path).
d /run/status/starport 0755 statuspull statuspull -
